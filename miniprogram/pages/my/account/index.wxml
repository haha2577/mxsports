<view class="page">

  <view class="header" style="padding-top:{{sbh+8}}px">
    <view class="nav-row">
      <view class="back-btn" bindtap="navigateBack"><text class="back-arrow">‹</text></view>
    </view>
    <text class="hd-emoji">👤</text>
    <view class="hd-text">
      <text class="hd-title">账号设置</text>
      <text class="hd-sub">管理你的个人信息</text>
    </view>
  </view>

  <!-- 头像 -->
  <view class="card">
    <text class="card-label">头像</text>
    <view class="avatar-wrap" bindtap="chooseAvatar">
      <image wx:if="{{avatar}}" class="avatar-img" src="{{avatar}}" mode="aspectFill"/>
      <view wx:else class="avatar-placeholder"><text class="ap-emoji">🏅</text></view>
      <view class="avatar-edit-badge"><text>📷</text></view>
    </view>
  </view>

  <!-- 昵称 -->
  <view class="card">
    <text class="card-label">昵称</text>
    <view class="input-row">
      <input class="nickname-input" value="{{nickname}}" bindinput="onNickname"
        placeholder="输入你的昵称" maxlength="20"/>
      <view wx:if="{{nicknameChanged}}" class="save-btn {{saving?'save-loading':''}}" bindtap="saveNickname">
        <text>{{saving?'保存中':'保存'}}</text>
      </view>
    </view>
  </view>

  <!-- 危险区域 -->
  <view class="danger-zone">
    <view class="danger-btn" bindtap="deleteAccount">
      <text class="danger-icon">⚠️</text>
      <text class="danger-label">注销账号</text>
      <text class="danger-sub">删除所有数据，不可恢复</text>
    </view>
  </view>

</view>
